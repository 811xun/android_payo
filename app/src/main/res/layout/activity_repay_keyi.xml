<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="23dp"
        android:layout_marginTop="@dimen/dp_45"
        android:text="Payment processing"
        android:textColor="@color/black"
        android:textSize="26sp" />

    <TextView
        android:id="@+id/msg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title"
        android:layout_marginLeft="23dp"
        android:layout_marginTop="25dp"
        android:layout_marginRight="23dp"
        android:layout_marginBottom="@dimen/dp_120"
        android:text="Your payment is now being processed through bank. Please check your final payment status inside the wallet. The transaction will show as ‘pending’ on your wallet until the payment process is complete."
        android:textColor="@color/black"
        android:textSize="@dimen/sp_15" />

    <TextView
        android:id="@+id/done"
        android:layout_width="@dimen/dp_313"
        android:layout_height="@dimen/dp_85"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="55dp"
        android:background="@mipmap/btn_orange_img"
        android:gravity="center"
        android:text="Done"
        android:textColor="@color/white"
        android:textSize="@dimen/sp_20" />

</RelativeLayout>